{
  "name": "hackathon",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "format-fix": "prettier -w .",
    "format-check": "prettier -c ."
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
